diff --git a/CHANGELOG.md b/CHANGELOG.md
index e094418..0ba4bf8 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -3,6 +3,27 @@
 All notable changes to Mappr. Format loosely follows
 [Keep a Changelog](https://keepachangelog.com/); versions follow semver.
 
+## [0.9.0] - 2026-09-15
+
+### Added
+- **Spread**, in the style panel: how the root's branches fan out. Six modes.
+  `As placed` is every previous version's behaviour and stays the default, so no
+  existing map changes shape. `Right and left`, `Top and bottom` and `All around`
+  give the classic mindmap shapes; `All right` and `All bottom` turn the same map
+  into a left-to-right tree or a top-down org chart.
+
+  It is derived, not applied. The directions are computed at layout time rather
+  than written into the nodes, so flipping between modes is a view change you can
+  do freely and reverse: going back to `As placed` returns the map to exactly the
+  placement you gave it. Branching *against* the current mode (pressing `⌘↑` in
+  `All right`, say) hands control back: the directions on screen are written in,
+  the map drops to `As placed`, nothing moves, and your placement lands.
+  Branching *with* the mode is not a takeover.
+
+  Direction now has one source of truth for the whole app, so navigation,
+  `Tab` cycling, `⌥`+arrow moves and insert-between all follow what is on
+  screen rather than what was once stored.
+
 ## [0.8.3] - 2026-09-15
 
 ### Added
diff --git a/README.md b/README.md
index 19d21cd..3bfbad0 100644
--- a/README.md
+++ b/README.md
@@ -98,6 +98,7 @@ One file, one IIFE, no dependencies. Roughly in reading order:
 
 | Section | What lives there |
 | --- | --- |
+| **Spread** | `SPREAD` maps each mode to the list of directions handed to the root's children in order. `spreadDirs()` derives `edir` for the whole tree each layout and `dirOf()` is the single source of truth everywhere else. Nothing is written into the nodes until `takeOverSpread()`, which fires only when a branch is placed against the mode. |
 | **Settings** | `DEFAULTS` / `CFG`, the palettes, and the size / spacing tables. Every visual choice in the app is a key on `CFG`. |
 | **Model** | `state = { rootId, nodes, frames }`. A node is `{id, parent, children[], text, dir, collapsed, mark}`. `dir` is `L`/`R`/`U`/`D` and governs which way its subtree grows. |
 | **Mutations** | `addNode`, `removeNode`, `branch`, `navigate`, undo/redo snapshots. |
diff --git a/VERSION b/VERSION
index ee94dd8..ac39a10 100644
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-0.8.3
+0.9.0
diff --git a/index.html b/index.html
index 6de6603..976e2ec 100644
--- a/index.html
+++ b/index.html
@@ -376,7 +376,7 @@
 </div>
 
 <div id="topbar" class="bar">
-  <span class="brand" id="brand" title="Mappr v0.8.3">Mappr</span>
+  <span class="brand" id="brand" title="Mappr v0.9.0">Mappr</span>
   <span class="sep"></span>
   <button class="tb" id="btnMaps">Maps ▾</button>
   <button class="tb" id="btnJump" title="Jump to a node (⌘K)">Jump ⌘K</button>
@@ -459,7 +459,7 @@
 
 <div id="help">
   <div id="helpCard">
-    <h2>Mappr keys <span style="font-family:var(--font-ui);font-size:11px;color:var(--muted)">v0.8.3</span></h2>
+    <h2>Mappr keys <span style="font-family:var(--font-ui);font-size:11px;color:var(--muted)">v0.9.0</span></h2>
     <p class="sub">Everything is reachable from the keyboard. Your hands never leave home row.</p>
 
     <h3>Create</h3>
@@ -537,13 +537,14 @@
 /* ============================================================
    SETTINGS
    ============================================================ */
-var VERSION="0.8.3";
+var VERSION="0.9.0";
 var DEFAULTS = {
   colorMode:"depth", stroke:"ink", fill:"paper", fillStyle:"solid",
   width:2, dash:"solid", slop:1, edges:"round",
   arrow:"curve", head:"arrow", font:"sketch", size:"m",
   align:"center", valign:"center", nodew:"m", minSize:false,
-  gap:"m", paper:"graph", theme:"light", autoEdit:true, follow:true
+  gap:"m", paper:"graph", theme:"light", autoEdit:true, follow:true,
+  spread:"manual"
 };
 var CFG = Object.assign({}, DEFAULTS);
 
@@ -612,7 +613,7 @@ function descCount(id){ var c=0; (function w(x){ N(x).children.forEach(function(
 function sibGroup(n){
   if(!n.parent) return [n.id];
   var p=N(n.parent);
-  if(isRoot(p)) return p.children.filter(function(c){ return N(c).dir===n.dir; });
+  if(isRoot(p)) return p.children.filter(function(c){ return dirOf(c)===dirOf(n.id); });
   return p.children.slice();
 }
 function branchIndex(n){
@@ -671,16 +672,17 @@ function removeNode(id){
 }
 function branch(node,pressed){
   if(isRoot(node)) return addNode(node.id,pressed,null);
-  if(pressed===node.dir) return addNode(node.id,node.dir,null);
-  if(pressed===OPP[node.dir]) return null;
+  var nd=dirOf(node.id);
+  if(pressed===nd) return addNode(node.id,nd,null);
+  if(pressed===OPP[nd]) return null;
   var p=N(node.parent), idx=p.children.indexOf(node.id);
-  return addNode(p.id,node.dir,(pressed==="U"||pressed==="L")?idx:idx+1);
+  return addNode(p.id,nd,(pressed==="U"||pressed==="L")?idx:idx+1);
 }
 function navigate(node,pressed){
   var to=null;
-  if(isRoot(node)) to=kids(node).filter(function(c){ return N(c).dir===pressed; })[0]||null;
-  else if(pressed===node.dir) to=kids(node)[0]||null;
-  else if(pressed===OPP[node.dir]) to=node.parent;
+  if(isRoot(node)) to=kids(node).filter(function(c){ return dirOf(c)===pressed; })[0]||null;
+  else if(pressed===dirOf(node.id)) to=kids(node)[0]||null;
+  else if(pressed===OPP[dirOf(node.id)]) to=node.parent;
   else { var g=sibGroup(node), i=g.indexOf(node.id); to=g[i+((pressed==="U"||pressed==="L")?-1:1)]||null; }
   return (to && visible(to)) ? to : null;
 }
@@ -728,12 +730,49 @@ function placeSubtree(id,d,mainC,crossC,memo){
     cursor+=ext+gapCross();
   });
 }
+/* How the root's branches fan out. Each mode is a list of directions handed to
+   the root's children in order; `manual` means no rule at all, so each node
+   keeps wherever it was put. The directions are DERIVED at layout time rather
+   than written into the nodes, so switching modes is a view change you can flip
+   back and forth, never an edit. */
+var SPREAD={
+  manual:null,
+  sides:["R","L"],
+  updown:["D","U"],
+  around:["R","L","D","U"],
+  right:["R"],
+  down:["D"]
+};
+/* The direction every node is actually drawn and navigated with. Computed for
+   the whole tree, collapsed branches included, so behaviour matches the picture
+   even where the picture is not currently showing. */
+var edir={};
+function spreadDirs(){
+  edir={};
+  var root=N(VR()); if(!root) return;
+  var list=SPREAD[CFG.spread]||null;
+  root.children.forEach(function(c,i){
+    var d=list?list[i%list.length]:N(c).dir;
+    (function w(x,dd){ edir[x]=dd; N(x).children.forEach(function(k){ w(k,dd); }); })(c,d);
+  });
+}
+function dirOf(id){ var d=edir[id]; return d||(N(id)||{}).dir; }
+/* Branching against the current mode hands control back: the derived
+   directions are written into the nodes and the map drops to `As placed`, so
+   the picture does not move and the hand-placement lands. */
+function takeOverSpread(){
+  if(CFG.spread==="manual") return;
+  Object.keys(edir).forEach(function(id){ if(N(id)) N(id).dir=edir[id]; });
+  CFG.spread="manual";
+  if(typeof drawPanel==="function") drawPanel();
+}
 function layout(){
   pos={};
+  spreadDirs();
   var rid=VR(), root=N(rid);
   pos[rid]={cx:0,cy:0};
   var groups={};
-  kids(root).forEach(function(c){ var d=N(c).dir; (groups[d]=groups[d]||[]).push(c); });
+  kids(root).forEach(function(c){ var d=dirOf(c); (groups[d]=groups[d]||[]).push(c); });
   ["L","R","U","D"].forEach(function(d){
     var g=groups[d]; if(!g||!g.length) return;
     var memo={}, total=0;
@@ -982,7 +1021,7 @@ function drawBadges(){
     if(!el){ el=document.createElement("div"); el.dataset.b=id; elBadges.appendChild(el); bdEls[id]=el; }
     el.className="badge";
     el.textContent="+"+descCount(id);
-    var p=pos[id], b=box[id], d=isRoot(n)?"R":n.dir, x,y;
+    var p=pos[id], b=box[id], d=isRoot(n)?"R":dirOf(id), x,y;
     if(d==="R"){ x=p.cx+b.w/2+7; y=p.cy-10; }
     else if(d==="L"){ x=p.cx-b.w/2-31; y=p.cy-10; }
     else if(d==="D"){ x=p.cx-12; y=p.cy+b.h/2+7; }
@@ -1068,7 +1107,7 @@ function shapes(ids, opt){
   ids.forEach(function(id){
     N(id).children.forEach(function(cid){
       if(!pos[cid] || !inSet[cid]) return;
-      var d=N(cid).dir, col=paletteFor(N(cid)).line, key=null;
+      var d=dirOf(cid), col=paletteFor(N(cid)).line, key=null;
       if(cache){
         key=cid+"|"+SC.gen+"|"+geo(id)+"|"+geo(cid)+"|"+d+"|"+col;
         var hit=SC.edge[key];
@@ -1425,7 +1464,11 @@ function select(id){
   clearMarks(); selected=id; render();
 }
 function doBranch(pressed){
-  var node=N(selected); snap();
+  var node=N(selected);
+  /* Only when the press disagrees with the mode. Pressing right in "All right"
+     is not a takeover, it is the mode doing its job. */
+  if(isVR(node) && CFG.spread!=="manual" && (SPREAD[CFG.spread]||[]).indexOf(pressed)<0) takeOverSpread();
+  snap();
   var made=branch(node,pressed);
   if(!made){ undoPop(); flash(selected); return; }
   clearMarks(); selected=made.id; render(false,pressed);
@@ -1435,8 +1478,8 @@ function doBranch(pressed){
 function doSibling(){
   var node=N(selected); snap();
   var made;
-  if(isVR(node)) made=addNode(node.id,isRoot(node)?"R":node.dir,null);
-  else { var p=N(node.parent); made=addNode(p.id,node.dir,p.children.indexOf(node.id)+1); }
+  if(isVR(node)) made=addNode(node.id,isRoot(node)?"R":dirOf(node.id),null);
+  else { var p=N(node.parent); made=addNode(p.id,dirOf(node.id),p.children.indexOf(node.id)+1); }
   clearMarks(); selected=made.id; render(false,made.dir);
   beginEdit(made.id,true); saveSoon();
 }
@@ -1448,29 +1491,29 @@ function doNextBranch(){
   snap();
   if(p.id===VR()){
     var order=["R","D","L","U"];
-    var have=order.filter(function(d){ return p.children.some(function(c){ return N(c).dir===d; }); });
-    var nd=n.dir;
-    if(have.length>1){ var i=have.indexOf(n.dir); nd=have[(i+1)%have.length]; }
+    var have=order.filter(function(d){ return p.children.some(function(c){ return dirOf(c)===d; }); });
+    var nd=dirOf(n.id);
+    if(have.length>1){ var i=have.indexOf(dirOf(n.id)); nd=have[(i+1)%have.length]; }
     made=addNode(p.id,nd,null);
   } else {
     var row=levelOrder(depthRel(p));
     var i2=row.indexOf(p.id);
     var q=(i2<0)?p.id:row[(i2+1)%row.length];
-    made=addNode(q,N(q).dir,null);
+    made=addNode(q,dirOf(q),null);
   }
   clearMarks(); selected=made.id; render(false,made.dir);
   beginEdit(made.id,true); saveSoon();
 }
-function doChild(){ var n=N(selected); doBranch(isRoot(n)?"R":n.dir); }
+function doChild(){ var n=N(selected); doBranch(isRoot(n)?"R":dirOf(n.id)); }
 /* hold Tab + arrow: slide a node in between */
 function insertBetween(dir){
   var n=N(selected);
-  var backward = !isVR(n) && dir===OPP[n.dir];
-  var forward  = isVR(n) ? true : dir===n.dir;
+  var backward = !isVR(n) && dir===OPP[dirOf(n.id)];
+  var forward  = isVR(n) ? true : dir===dirOf(n.id);
   if(backward){
     snap();
     var p=N(n.parent), idx=p.children.indexOf(n.id);
-    var mid=addNode(p.id,n.dir,idx);
+    var mid=addNode(p.id,dirOf(n.id),idx);
     p.children.splice(p.children.indexOf(n.id),1);
     n.parent=mid.id; mid.children.push(n.id);
     state.frames.forEach(function(f){ f.roots=f.roots.map(function(r){ return r===n.id?mid.id:r; }); });
@@ -1479,7 +1522,7 @@ function insertBetween(dir){
     return;
   }
   if(forward){
-    var kids = isRoot(n) ? n.children.filter(function(c){ return N(c).dir===dir; }) : n.children.slice();
+    var kids = isRoot(n) ? n.children.filter(function(c){ return dirOf(c)===dir; }) : n.children.slice();
     snap();
     var mid2=addNode(n.id,dir,null);
     kids.forEach(function(c){
@@ -1506,22 +1549,22 @@ function moveNode(dir){
   var n=N(selected);
   if(isRoot(n)||isVR(n)){ flash(selected); return; }
   var p=N(n.parent);
-  if(dir===n.dir){
+  if(dir===dirOf(n.id)){
     var g=sibGroup(n), i=g.indexOf(n.id);
     if(i<=0){ flash(selected); return; }
     var host=N(g[i-1]);
     snap();
     p.children.splice(p.children.indexOf(n.id),1);
     n.parent=host.id; host.children.push(n.id); host.collapsed=false;
-    setDirDeep(n.id, host.dir);
-  } else if(dir===OPP[n.dir]){
+    setDirDeep(n.id, dirOf(host.id));
+  } else if(dir===OPP[dirOf(n.id)]){
     if(p.id===VR()||isRoot(p)){ flash(selected); return; }
     var gp=N(p.parent);
     snap();
     p.children.splice(p.children.indexOf(n.id),1);
     n.parent=gp.id;
     gp.children.splice(gp.children.indexOf(p.id)+1,0,n.id);
-    setDirDeep(n.id, p.dir);
+    setDirDeep(n.id, dirOf(p.id));
   } else {
     var g2=sibGroup(n), i2=g2.indexOf(n.id);
     var j=i2+((dir==="U"||dir==="L")?-1:1);
@@ -2159,6 +2202,12 @@ addEventListener("mousedown",function(e){
 function svg(inner){ return '<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">'+inner+'</svg>'; }
 function lt(t,extra){ return '<span class="lt"'+(extra?' style="'+extra+'"':"")+'>'+t+'</span>'; }
 var I={
+  spman:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M8.3 9.2 4.6 7.4M11.7 10.8 15.6 12.6M10.4 8.2 11.6 4.4"/>'),
+  spsides:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M8.1 9.2 4.6 6.6M8.1 10.8 4.6 13.4M11.9 9.2 15.4 6.6M11.9 10.8 15.4 13.4"/>'),
+  spud:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M9.2 8.1 6.6 4.6M10.8 8.1 13.4 4.6M9.2 11.9 6.6 15.4M10.8 11.9 13.4 15.4"/>'),
+  spall:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M8.1 10H4.4M11.9 10h3.7M10 8.1V4.4M10 11.9v3.7"/>'),
+  spr:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M11.9 10h3.7M11.9 9.6 15.6 6.2M11.9 10.4 15.6 13.8"/>'),
+  spd:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M10 11.9v3.7M9.6 11.9 6.2 15.6M10.4 11.9 13.8 15.6"/>'),
   depth:svg('<circle cx="4" cy="10" r="2.2" fill="currentColor" stroke="none" opacity=".95"/><circle cx="10" cy="10" r="2.2" fill="currentColor" stroke="none" opacity=".6"/><circle cx="16" cy="10" r="2.2" fill="currentColor" stroke="none" opacity=".3"/>'),
   branchc:svg('<path d="M3 10h4M7 4.5v11M7 4.5h4M7 15.5h4"/><circle cx="14.5" cy="4.5" r="2" fill="currentColor" stroke="none" opacity=".9"/><circle cx="14.5" cy="15.5" r="2" fill="currentColor" stroke="none" opacity=".45"/>'),
   onec:svg('<circle cx="10" cy="10" r="5.5" fill="currentColor" stroke="none"/>'),
@@ -2190,6 +2239,14 @@ var I={
   tdark:svg('<path d="M15.5 12.2A6.4 6.4 0 0 1 7.8 4.5a6.4 6.4 0 1 0 7.7 7.7z"/>')
 };
 var PANEL=[
+  {h:"Spread",key:"spread",opts:[
+    ["manual",I.spman,"As placed"],
+    ["sides",I.spsides,"Right and left"],
+    ["updown",I.spud,"Top and bottom"],
+    ["around",I.spall,"All around"],
+    ["right",I.spr,"All right"],
+    ["down",I.spd,"All bottom"]
+  ]},
   {h:"Colour",key:"colorMode",opts:[["depth",I.depth,"Colour by depth"],["branch",I.branchc,"Colour by branch"],["one",I.onec,"One colour"]]},
   {h:"Stroke",key:"stroke",swatch:STROKES,when:function(){ return CFG.colorMode==="one"; }},
   {h:"Background",key:"fill",swatch:FILLS,when:function(){ return CFG.colorMode==="one"; }},
@@ -2697,6 +2754,7 @@ window.__mf={
   navigate:function(d){ var t=navigate(N(selected),d); if(t) select(t); return t; },
   tab:function(b){ var x=cycleLevel(N(selected),b); if(x) select(x); return x; },
   focusIn:enterFocus, focusOut:exitFocus,
+  dir:dirOf, spread:function(v){ if(v===undefined) return CFG.spread; CFG.spread=v; drawPanel(); render(true); save(); },
   outline:currentOutline, outlineHtml:currentOutlineHtml,
   paste:function(t){ return insertOutline(parseOutline(t)); },
   pasteRich:function(html,text){ return insertOutline(bestOutline(html,text||"")); },
diff --git a/src/app.html b/src/app.html
index 6999e2d..01e349d 100644
--- a/src/app.html
+++ b/src/app.html
@@ -543,7 +543,8 @@ var DEFAULTS = {
   width:2, dash:"solid", slop:1, edges:"round",
   arrow:"curve", head:"arrow", font:"sketch", size:"m",
   align:"center", valign:"center", nodew:"m", minSize:false,
-  gap:"m", paper:"graph", theme:"light", autoEdit:true, follow:true
+  gap:"m", paper:"graph", theme:"light", autoEdit:true, follow:true,
+  spread:"manual"
 };
 var CFG = Object.assign({}, DEFAULTS);
 
@@ -612,7 +613,7 @@ function descCount(id){ var c=0; (function w(x){ N(x).children.forEach(function(
 function sibGroup(n){
   if(!n.parent) return [n.id];
   var p=N(n.parent);
-  if(isRoot(p)) return p.children.filter(function(c){ return N(c).dir===n.dir; });
+  if(isRoot(p)) return p.children.filter(function(c){ return dirOf(c)===dirOf(n.id); });
   return p.children.slice();
 }
 function branchIndex(n){
@@ -671,16 +672,17 @@ function removeNode(id){
 }
 function branch(node,pressed){
   if(isRoot(node)) return addNode(node.id,pressed,null);
-  if(pressed===node.dir) return addNode(node.id,node.dir,null);
-  if(pressed===OPP[node.dir]) return null;
+  var nd=dirOf(node.id);
+  if(pressed===nd) return addNode(node.id,nd,null);
+  if(pressed===OPP[nd]) return null;
   var p=N(node.parent), idx=p.children.indexOf(node.id);
-  return addNode(p.id,node.dir,(pressed==="U"||pressed==="L")?idx:idx+1);
+  return addNode(p.id,nd,(pressed==="U"||pressed==="L")?idx:idx+1);
 }
 function navigate(node,pressed){
   var to=null;
-  if(isRoot(node)) to=kids(node).filter(function(c){ return N(c).dir===pressed; })[0]||null;
-  else if(pressed===node.dir) to=kids(node)[0]||null;
-  else if(pressed===OPP[node.dir]) to=node.parent;
+  if(isRoot(node)) to=kids(node).filter(function(c){ return dirOf(c)===pressed; })[0]||null;
+  else if(pressed===dirOf(node.id)) to=kids(node)[0]||null;
+  else if(pressed===OPP[dirOf(node.id)]) to=node.parent;
   else { var g=sibGroup(node), i=g.indexOf(node.id); to=g[i+((pressed==="U"||pressed==="L")?-1:1)]||null; }
   return (to && visible(to)) ? to : null;
 }
@@ -728,12 +730,49 @@ function placeSubtree(id,d,mainC,crossC,memo){
     cursor+=ext+gapCross();
   });
 }
+/* How the root's branches fan out. Each mode is a list of directions handed to
+   the root's children in order; `manual` means no rule at all, so each node
+   keeps wherever it was put. The directions are DERIVED at layout time rather
+   than written into the nodes, so switching modes is a view change you can flip
+   back and forth, never an edit. */
+var SPREAD={
+  manual:null,
+  sides:["R","L"],
+  updown:["D","U"],
+  around:["R","L","D","U"],
+  right:["R"],
+  down:["D"]
+};
+/* The direction every node is actually drawn and navigated with. Computed for
+   the whole tree, collapsed branches included, so behaviour matches the picture
+   even where the picture is not currently showing. */
+var edir={};
+function spreadDirs(){
+  edir={};
+  var root=N(VR()); if(!root) return;
+  var list=SPREAD[CFG.spread]||null;
+  root.children.forEach(function(c,i){
+    var d=list?list[i%list.length]:N(c).dir;
+    (function w(x,dd){ edir[x]=dd; N(x).children.forEach(function(k){ w(k,dd); }); })(c,d);
+  });
+}
+function dirOf(id){ var d=edir[id]; return d||(N(id)||{}).dir; }
+/* Branching against the current mode hands control back: the derived
+   directions are written into the nodes and the map drops to `As placed`, so
+   the picture does not move and the hand-placement lands. */
+function takeOverSpread(){
+  if(CFG.spread==="manual") return;
+  Object.keys(edir).forEach(function(id){ if(N(id)) N(id).dir=edir[id]; });
+  CFG.spread="manual";
+  if(typeof drawPanel==="function") drawPanel();
+}
 function layout(){
   pos={};
+  spreadDirs();
   var rid=VR(), root=N(rid);
   pos[rid]={cx:0,cy:0};
   var groups={};
-  kids(root).forEach(function(c){ var d=N(c).dir; (groups[d]=groups[d]||[]).push(c); });
+  kids(root).forEach(function(c){ var d=dirOf(c); (groups[d]=groups[d]||[]).push(c); });
   ["L","R","U","D"].forEach(function(d){
     var g=groups[d]; if(!g||!g.length) return;
     var memo={}, total=0;
@@ -982,7 +1021,7 @@ function drawBadges(){
     if(!el){ el=document.createElement("div"); el.dataset.b=id; elBadges.appendChild(el); bdEls[id]=el; }
     el.className="badge";
     el.textContent="+"+descCount(id);
-    var p=pos[id], b=box[id], d=isRoot(n)?"R":n.dir, x,y;
+    var p=pos[id], b=box[id], d=isRoot(n)?"R":dirOf(id), x,y;
     if(d==="R"){ x=p.cx+b.w/2+7; y=p.cy-10; }
     else if(d==="L"){ x=p.cx-b.w/2-31; y=p.cy-10; }
     else if(d==="D"){ x=p.cx-12; y=p.cy+b.h/2+7; }
@@ -1068,7 +1107,7 @@ function shapes(ids, opt){
   ids.forEach(function(id){
     N(id).children.forEach(function(cid){
       if(!pos[cid] || !inSet[cid]) return;
-      var d=N(cid).dir, col=paletteFor(N(cid)).line, key=null;
+      var d=dirOf(cid), col=paletteFor(N(cid)).line, key=null;
       if(cache){
         key=cid+"|"+SC.gen+"|"+geo(id)+"|"+geo(cid)+"|"+d+"|"+col;
         var hit=SC.edge[key];
@@ -1425,7 +1464,11 @@ function select(id){
   clearMarks(); selected=id; render();
 }
 function doBranch(pressed){
-  var node=N(selected); snap();
+  var node=N(selected);
+  /* Only when the press disagrees with the mode. Pressing right in "All right"
+     is not a takeover, it is the mode doing its job. */
+  if(isVR(node) && CFG.spread!=="manual" && (SPREAD[CFG.spread]||[]).indexOf(pressed)<0) takeOverSpread();
+  snap();
   var made=branch(node,pressed);
   if(!made){ undoPop(); flash(selected); return; }
   clearMarks(); selected=made.id; render(false,pressed);
@@ -1435,8 +1478,8 @@ function doBranch(pressed){
 function doSibling(){
   var node=N(selected); snap();
   var made;
-  if(isVR(node)) made=addNode(node.id,isRoot(node)?"R":node.dir,null);
-  else { var p=N(node.parent); made=addNode(p.id,node.dir,p.children.indexOf(node.id)+1); }
+  if(isVR(node)) made=addNode(node.id,isRoot(node)?"R":dirOf(node.id),null);
+  else { var p=N(node.parent); made=addNode(p.id,dirOf(node.id),p.children.indexOf(node.id)+1); }
   clearMarks(); selected=made.id; render(false,made.dir);
   beginEdit(made.id,true); saveSoon();
 }
@@ -1448,29 +1491,29 @@ function doNextBranch(){
   snap();
   if(p.id===VR()){
     var order=["R","D","L","U"];
-    var have=order.filter(function(d){ return p.children.some(function(c){ return N(c).dir===d; }); });
-    var nd=n.dir;
-    if(have.length>1){ var i=have.indexOf(n.dir); nd=have[(i+1)%have.length]; }
+    var have=order.filter(function(d){ return p.children.some(function(c){ return dirOf(c)===d; }); });
+    var nd=dirOf(n.id);
+    if(have.length>1){ var i=have.indexOf(dirOf(n.id)); nd=have[(i+1)%have.length]; }
     made=addNode(p.id,nd,null);
   } else {
     var row=levelOrder(depthRel(p));
     var i2=row.indexOf(p.id);
     var q=(i2<0)?p.id:row[(i2+1)%row.length];
-    made=addNode(q,N(q).dir,null);
+    made=addNode(q,dirOf(q),null);
   }
   clearMarks(); selected=made.id; render(false,made.dir);
   beginEdit(made.id,true); saveSoon();
 }
-function doChild(){ var n=N(selected); doBranch(isRoot(n)?"R":n.dir); }
+function doChild(){ var n=N(selected); doBranch(isRoot(n)?"R":dirOf(n.id)); }
 /* hold Tab + arrow: slide a node in between */
 function insertBetween(dir){
   var n=N(selected);
-  var backward = !isVR(n) && dir===OPP[n.dir];
-  var forward  = isVR(n) ? true : dir===n.dir;
+  var backward = !isVR(n) && dir===OPP[dirOf(n.id)];
+  var forward  = isVR(n) ? true : dir===dirOf(n.id);
   if(backward){
     snap();
     var p=N(n.parent), idx=p.children.indexOf(n.id);
-    var mid=addNode(p.id,n.dir,idx);
+    var mid=addNode(p.id,dirOf(n.id),idx);
     p.children.splice(p.children.indexOf(n.id),1);
     n.parent=mid.id; mid.children.push(n.id);
     state.frames.forEach(function(f){ f.roots=f.roots.map(function(r){ return r===n.id?mid.id:r; }); });
@@ -1479,7 +1522,7 @@ function insertBetween(dir){
     return;
   }
   if(forward){
-    var kids = isRoot(n) ? n.children.filter(function(c){ return N(c).dir===dir; }) : n.children.slice();
+    var kids = isRoot(n) ? n.children.filter(function(c){ return dirOf(c)===dir; }) : n.children.slice();
     snap();
     var mid2=addNode(n.id,dir,null);
     kids.forEach(function(c){
@@ -1506,22 +1549,22 @@ function moveNode(dir){
   var n=N(selected);
   if(isRoot(n)||isVR(n)){ flash(selected); return; }
   var p=N(n.parent);
-  if(dir===n.dir){
+  if(dir===dirOf(n.id)){
     var g=sibGroup(n), i=g.indexOf(n.id);
     if(i<=0){ flash(selected); return; }
     var host=N(g[i-1]);
     snap();
     p.children.splice(p.children.indexOf(n.id),1);
     n.parent=host.id; host.children.push(n.id); host.collapsed=false;
-    setDirDeep(n.id, host.dir);
-  } else if(dir===OPP[n.dir]){
+    setDirDeep(n.id, dirOf(host.id));
+  } else if(dir===OPP[dirOf(n.id)]){
     if(p.id===VR()||isRoot(p)){ flash(selected); return; }
     var gp=N(p.parent);
     snap();
     p.children.splice(p.children.indexOf(n.id),1);
     n.parent=gp.id;
     gp.children.splice(gp.children.indexOf(p.id)+1,0,n.id);
-    setDirDeep(n.id, p.dir);
+    setDirDeep(n.id, dirOf(p.id));
   } else {
     var g2=sibGroup(n), i2=g2.indexOf(n.id);
     var j=i2+((dir==="U"||dir==="L")?-1:1);
@@ -2159,6 +2202,12 @@ addEventListener("mousedown",function(e){
 function svg(inner){ return '<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">'+inner+'</svg>'; }
 function lt(t,extra){ return '<span class="lt"'+(extra?' style="'+extra+'"':"")+'>'+t+'</span>'; }
 var I={
+  spman:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M8.3 9.2 4.6 7.4M11.7 10.8 15.6 12.6M10.4 8.2 11.6 4.4"/>'),
+  spsides:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M8.1 9.2 4.6 6.6M8.1 10.8 4.6 13.4M11.9 9.2 15.4 6.6M11.9 10.8 15.4 13.4"/>'),
+  spud:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M9.2 8.1 6.6 4.6M10.8 8.1 13.4 4.6M9.2 11.9 6.6 15.4M10.8 11.9 13.4 15.4"/>'),
+  spall:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M8.1 10H4.4M11.9 10h3.7M10 8.1V4.4M10 11.9v3.7"/>'),
+  spr:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M11.9 10h3.7M11.9 9.6 15.6 6.2M11.9 10.4 15.6 13.8"/>'),
+  spd:svg('<circle cx="10" cy="10" r="1.9" fill="currentColor" stroke="none"/><path d="M10 11.9v3.7M9.6 11.9 6.2 15.6M10.4 11.9 13.8 15.6"/>'),
   depth:svg('<circle cx="4" cy="10" r="2.2" fill="currentColor" stroke="none" opacity=".95"/><circle cx="10" cy="10" r="2.2" fill="currentColor" stroke="none" opacity=".6"/><circle cx="16" cy="10" r="2.2" fill="currentColor" stroke="none" opacity=".3"/>'),
   branchc:svg('<path d="M3 10h4M7 4.5v11M7 4.5h4M7 15.5h4"/><circle cx="14.5" cy="4.5" r="2" fill="currentColor" stroke="none" opacity=".9"/><circle cx="14.5" cy="15.5" r="2" fill="currentColor" stroke="none" opacity=".45"/>'),
   onec:svg('<circle cx="10" cy="10" r="5.5" fill="currentColor" stroke="none"/>'),
@@ -2190,6 +2239,14 @@ var I={
   tdark:svg('<path d="M15.5 12.2A6.4 6.4 0 0 1 7.8 4.5a6.4 6.4 0 1 0 7.7 7.7z"/>')
 };
 var PANEL=[
+  {h:"Spread",key:"spread",opts:[
+    ["manual",I.spman,"As placed"],
+    ["sides",I.spsides,"Right and left"],
+    ["updown",I.spud,"Top and bottom"],
+    ["around",I.spall,"All around"],
+    ["right",I.spr,"All right"],
+    ["down",I.spd,"All bottom"]
+  ]},
   {h:"Colour",key:"colorMode",opts:[["depth",I.depth,"Colour by depth"],["branch",I.branchc,"Colour by branch"],["one",I.onec,"One colour"]]},
   {h:"Stroke",key:"stroke",swatch:STROKES,when:function(){ return CFG.colorMode==="one"; }},
   {h:"Background",key:"fill",swatch:FILLS,when:function(){ return CFG.colorMode==="one"; }},
@@ -2697,6 +2754,7 @@ window.__mf={
   navigate:function(d){ var t=navigate(N(selected),d); if(t) select(t); return t; },
   tab:function(b){ var x=cycleLevel(N(selected),b); if(x) select(x); return x; },
   focusIn:enterFocus, focusOut:exitFocus,
+  dir:dirOf, spread:function(v){ if(v===undefined) return CFG.spread; CFG.spread=v; drawPanel(); render(true); save(); },
   outline:currentOutline, outlineHtml:currentOutlineHtml,
   paste:function(t){ return insertOutline(parseOutline(t)); },
   pasteRich:function(html,text){ return insertOutline(bestOutline(html,text||"")); },
diff --git a/sw.js b/sw.js
index be7a5b4..edb0e41 100644
--- a/sw.js
+++ b/sw.js
@@ -9,7 +9,7 @@
  * so it can offer a reload. Everything else is cache-first, since the icons and
  * the manifest never change within a version.
  */
-var VERSION = "0.8.3";
+var VERSION = "0.9.0";
 var CACHE = "mappr-v" + VERSION;
 var SHELL = [
   "./",
diff --git a/tests/smoke.mjs b/tests/smoke.mjs
index 1a6c646..f00b2a0 100644
--- a/tests/smoke.mjs
+++ b/tests/smoke.mjs
@@ -358,6 +358,66 @@ group("outlines from rendered documents");
   ok("plain text still wins when the html has no list", (await shape()) === WANT, await shape());
 }
 
+group("spread");
+{
+  const newMap = async () => {
+    await M(() => document.getElementById("btnMaps").click());
+    await M(() => document.getElementById("btnNewMap").click());
+    await page.waitForTimeout(200);
+  };
+  const rootDirs = () => M(() => __mf.state.nodes[__mf.state.rootId].children.map((c) => __mf.dir(c)).join(","));
+  const setSpread = async (v) => { await M((x) => __mf.spread(x), v); await page.waitForTimeout(200); };
+
+  await newMap();
+  await M(() => __mf.paste("Map\n- A\n- B\n- C\n- D\n- E"));
+  await page.waitForTimeout(250);
+  ok("a new map spreads as placed", (await M(() => __mf.spread())) === "manual");
+  const asPlaced = await rootDirs();
+  const outline0 = await M(() => __mf.outline());
+  const count0 = await M(() => Object.keys(__mf.state.nodes).length);
+
+  for (const [mode, want] of [
+    ["sides", "R,L,R,L,R"],
+    ["updown", "D,U,D,U,D"],
+    ["around", "R,L,D,U,R"],
+    ["right", "R,R,R,R,R"],
+    ["down", "D,D,D,D,D"],
+  ]) {
+    await setSpread(mode);
+    ok("spread " + mode + " fans the branches", (await rootDirs()) === want, await rootDirs());
+    ok("spread " + mode + " lays every node out", (await M(() => Object.keys(__mf.pos()).length)) === count0);
+  }
+
+  // The whole point of deriving rather than applying: it is a view, not an edit.
+  ok("no spread changed the map", (await M(() => __mf.outline())) === outline0);
+  ok("no spread added or lost a node", (await M(() => Object.keys(__mf.state.nodes).length)) === count0);
+  await setSpread("manual");
+  ok("going back restores the original placement", (await rootDirs()) === asPlaced, { now: await rootDirs(), was: asPlaced });
+
+  // Branching against the mode hands control back; branching with it does not.
+  await setSpread("right");
+  await M(() => __mf.select(__mf.state.rootId));
+  await M(() => __mf.branch("R"));
+  await key("Escape");
+  await page.waitForTimeout(150);
+  ok("branching with the mode keeps it", (await M(() => __mf.spread())) === "right");
+  await M(() => __mf.select(__mf.state.rootId));
+  await M(() => __mf.branch("U"));
+  await key("Escape");
+  await page.waitForTimeout(150);
+  ok("branching against the mode drops to as placed", (await M(() => __mf.spread())) === "manual");
+  ok("the takeover keeps everything else where it was",
+    (await rootDirs()).split(",").slice(0, 5).join(",") === "R,R,R,R,R", await rootDirs());
+
+  // A single-direction spread must survive a reload, like any other setting.
+  await setSpread("down");
+  await page.waitForTimeout(600);
+  await page.reload();
+  await page.waitForFunction(() => !!window.__mf);
+  await page.waitForTimeout(300);
+  ok("spread is saved with the map", (await M(() => __mf.spread())) === "down");
+}
+
 group("copying an outline");
 {
   const newMap = async () => {
